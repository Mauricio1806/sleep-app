import { Region } from '../types';

export function isPremiumEnabled(region: Region): boolean {
  return region === 'BR';
}

export function isB2BEnabled(region: Region): boolean {
  return region === 'BR';
}

export function isCommunityEnabled(region: Region): boolean {
  return region === 'BR';
}

export function isWearableEnabled(region: Region): boolean {
  return region === 'BR';
}
